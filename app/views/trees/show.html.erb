<p style="color: green"><%= notice %></p>

<%= render @tree %>

<div>
  <%= link_to "Edit this tree", edit_tree_path(@tree) %> |
  <%= link_to "Back to trees", trees_path %>
  <% if current_user.id.to_i == @tree.user_id.to_i %>
    <%= button_to "Destroy this tree", @tree, method: :delete %>
  <%end%>
</div>
